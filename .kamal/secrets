# Secrets for Kamal deploy. Evaluated by the shell — do not put literal secrets in git.
RAILS_MASTER_KEY=$(cat config/master.key)
POSTGRES_PASSWORD=$(cat .kamal/postgres_password)
DATABASE_URL=postgres://beastmode:$(cat .kamal/postgres_password)@beastmode-db:5432/beastmode_production
